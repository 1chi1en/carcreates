<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>Agency - Start Bootstrap Theme</title>
        <link rel="icon" type="image/x-icon" href="assets/img/favicon.ico" />
        <!-- Font Awesome icons (free version)-->
        <script src="https://use.fontawesome.com/releases/v5.15.1/js/all.js" crossorigin="anonymous"></script>
        <!-- Google fonts-->
        <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css" />
        <link href="https://fonts.googleapis.com/css?family=Droid+Serif:400,700,400italic,700italic" rel="stylesheet" type="text/css" />
        <link href="https://fonts.googleapis.com/css?family=Roboto+Slab:400,100,300,700" rel="stylesheet" type="text/css" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="css/styles.css" rel="stylesheet" />
    </head>
    <body id="page-top">

    <%= render "shared/header" %>

        <!-- Masthead-->
    <div class="backimage">
        <header class="masthead">
            <div class="container">
                <div class="masthead-heading">CAR-CREATES</div>
                <div class="masthead-subheading">
                    <p>カスタムカーや競技車両の製作</p>
                    <p>サーキット向けの車両セッティング</p>
                    <p>旧車のレストア</p>
                    <p>などのノウハウを投稿して共有できます</p>
                </div>
                <%# <a class="btn btn-primary btn-xl text-uppercase js-scroll-trigger" href="#services">Tell Me More</a> %>
            </div>
        </header>
    </div>



        <!-- Portfolio Grid-->
        <section class="page-section bg-light" id="portfolio">
            <div class="container">
                <div class="text-center">
                    <h3 class="section-heading text-uppercase">投稿記事一覧</h3>
                    <h3 class="section-subheading text-muted"></h3>
                </div>
                <div class="row">
                    <% @articles.each do |article| %>
                        <div class="col-lg-4 col-sm-6 mb-4">
                            <div class="portfolio-item">
                                <%= link_to article_path(article.id), class: "toplink" do %>
                                    <div class="portfolio-caption">
                                    <%= image_tag article.image.variant(combine_options:{gravity: :center, resize:"200x200^",crop:"200x200+0+0"}).processed, class: "item-img" if article.image.attached? %>
                                        <div class="portfolio-caption-heading"><%= article.title %></div>
                                        <div class="portfolio-caption-subheading text-muted"><%= article.content.to_plain_text.truncate(20) %></div>
                                    </div>
                                <% end %>
                            </div>
                        </div>
                    <% end %>
                </div>
                <%= paginate @articles %>
            </div>
        </section>

        <%= render "shared/footer" %>


        <!-- Bootstrap core JS-->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Third party plugin JS-->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.4.1/jquery.easing.min.js"></script>
        <!-- Contact form JS-->
        <script src="assets/mail/jqBootstrapValidation.js"></script>
        <script src="assets/mail/contact_me.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>
    </body>
</html>
